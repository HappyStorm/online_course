#!/usr/bin/python

import sys

count = 0
# Loop around the data
# It will be in the format val
# Where val is the specific ip

for line in sys.stdin:
    new_ip = line.strip()

    if ip == '10.99.99.186'
        count += 1

print 'Hits: ', count
